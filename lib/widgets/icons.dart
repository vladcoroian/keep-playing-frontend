import 'package:flutter/material.dart';

class EventIcons {
  static const Icon NAME_ICON = Icon(Icons.title);
  static const Icon SPORT_ICON = Icon(Icons.sports_soccer);
  static const Icon ROLE_ICON = Icon(Icons.sports);
  static const Icon LOCATION_ICON = Icon(Icons.location_on);
  static const Icon DETAILS_ICON = Icon(Icons.details);

  static const Icon DATE_ICON = Icon(Icons.date_range);
  static const Icon START_TIME_ICON = Icon(Icons.access_time);
  static const Icon END_TIME_ICON = Icon(Icons.access_time);

  static const Icon PRICE_ICON = Icon(Icons.price_change);
}

class UserIcons {
  static const Icon USERNAME_ICON = Icon(Icons.account_box);
  static const Icon EMAIL_ICON = Icon(Icons.email);
  static const Icon NAME_ICON = Icon(Icons.person);
  static const Icon FIRST_NAME_ICON = Icon(Icons.person);
  static const Icon LAST_NAME_ICON = Icon(Icons.person);
  static const Icon LOCATION_ICON = Icon(Icons.location_on);
}

class CoachIcons {
  static const Icon RATE_ICON = Icon(
    Icons.star,
    color: Colors.amber,
  );

  static const Icon FAVOURITE_ICON = Icon(
    Icons.favorite,
    color: Colors.red,
  );
  static const Icon VERIFIED_ICON = Icon(
    Icons.verified,
    color: Colors.blue,
  );
}
